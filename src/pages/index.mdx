---
navigationLabel: home
---

import { Link as GatsbyLink } from "gatsby"
import { mix } from "@theme-ui/color"
import { PostCard } from "../components/PostCard"

# [maxence-laumonier](#maxence-laumonier)

Data Scientist / Economist - Python, R, SQL, Javascript - based in Paris, France.

Python, R, JavaScript, SQL, Sklearn, PyTorch and a tiny bit of Spark.

<Button
  as="a"
  variant="success"
  href="https://wikipedia.org"
  target="_blank"
  sx={{
    mr: 2,
  }}
>
  View CV/Resume
</Button>

<Link
  href="https://wikipedia.org"
  target="_blank"
  title="View CV"
>
  .pdf version
</Link>
<Divider />
<Divider />

## [intro](#intro)

Hello there ! 👋 I'm Maxence and began my career as an economist, but after a few years of Powerpoints and Excels, I realised there's much more insights to be gained with a solid programming knowledge ! So I left my job, taught at the university in the meantime, and learned data science on the side.

<Divider />

<Flex
  sx={{ flexWrap: 'wrap'}}
>
<Box
  sx={{
    width: ["100%", "100%", "55%"],
    mb: 3,
    pr: [0,0, 4],
  }}
>

## contact

<Text>Email</Text>
<Link href="mailto:maxence@maxence.dev" target="_blank" variant="styles.a">
  maxence@maxence.dev
</Link>
<Divider />

<Text>GitHub</Text>
<Link
  href="https://github.com/Maxence-L"
  target="_blank"
  variant="styles.a"
>
  https://github.com/MaxenceL
</Link>
<Divider />

<Divider />
</Box>

<Box
  sx={{
    width: ["100%", "100%", "45%"],
    mb: 3
  }}
>

## blog topics

<Divider />

<SourceTags>
  {(tags) => (
    <Box>
      {tags
        .sort((a, b) => b.percent - a.percent)
        .map((tag, index) => {
          const { name, count, percent } = tag
          return (
            <Badge
              key={index}
              variant="primary"
              sx={{
                mr: 2,
                mb: 2,
                color: mix("secondary", "primary", `${index / tags.length}`),
                borderColor: mix(
                  "secondary",
                  "primary",
                  `${index / tags.length}`
                ),
              }}
            >
              {name}
            </Badge>
          )
        })}
    </Box>
  )}
</SourceTags>

<Divider />
<Divider />

</Box>
</Flex>

## latest posts

<Divider />

<SourceList filter="posts">
  {(posts) => (
    <Flex
      sx={{
        flexWrap: "wrap",
        ml: (theme) => `-${theme.space[2]}px`,
        mr: (theme) => `-${theme.space[2]}px`,
      }}
    >
      {posts
        .filter((edge) => !edge.node.frontmatter.isPrivate)
        .reduce((posts, post) => {
          return post.node.frontmatter.pinned
            ? [post, ...posts]
            : [...posts, post]
        }, [])
        .splice(0, 4)
        .map((edge, index) => {
          const {
            frontmatter: {
              title,
              featuredImageUrl,
              tags,
              date,
              dateModified,
              pinned,
            },
            excerpt,
            fields: { slug },
          } = edge.node
          return (
            <PostCard
              key={index}
              title={title}
              featuredImageUrl={featuredImageUrl}
              tags={tags}
              date={date}
              dateModified={dateModified}
              excerpt={excerpt}
              slug={slug}
              pinned={pinned}
            />
          )
        })}
    </Flex>
  )}
</SourceList>
<Divider />
<Flex
  sx={{
    justifyContent: "center",
  }}
>
  <Box>
    <Button as={GatsbyLink} variant="ghost" to="/posts">
      More posts{" "}
      <Box as="span" role="img" aria-label="pencil">
        ✏️
      </Box>
    </Button>
  </Box>
</Flex>
